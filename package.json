{
  "name": "talentbliss",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @talentbliss/web",
    "dev:api": "npm run dev --workspace @talentbliss/api",
    "build": "npm run build --workspace @talentbliss/web && npm run build --workspace @talentbliss/api",
    "lint": "npm run lint --workspace @talentbliss/web && npm run lint --workspace @talentbliss/api",
    "test:api": "npm run test --workspace @talentbliss/api",
    "test:pipeline": "PYTHONPATH=pipeline/src python3 -m unittest discover -s pipeline/tests -v",
    "test": "npm run test:api && npm run test:pipeline",
    "check": "npm run lint && npm run test && npm run build"
  }
}
